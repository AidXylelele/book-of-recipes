import { SPACES } from '../theme';

export class ProfileStyles {
  static container = {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: SPACES.l,
  };
  static card = {
    minWidth: 500
  };
  static media = {
    height: 140
  };
}
